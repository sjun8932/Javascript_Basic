const candidate = Array(45).fill().map((v,i) => {return i+1}) //const candidate = Array(45).fill().map((v,i) => i+1) , 걍 return을 없애서 줄여도 된다.

const shuffle = [];
while (candidate.length>0) {
  const value = Math.floor(Math.random() * candidate.length)
  shuffle.push()
}


