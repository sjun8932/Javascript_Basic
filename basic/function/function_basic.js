function add ( a , b, c ) {
    return a + b + c
}
// ⓐ 기본적인 함수의 구조


const add = ( a, b, c ) => {
   return a + b + c
}
// ⓑ 화살표 함수의 구조

//  ⓐ 와 ⓑ 는 같은 함수이고 표현만 다른 것이다.

  add(1, 2, 3)

  const d = add(5,7,9)